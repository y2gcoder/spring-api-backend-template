spring:
  profiles:
    active: test
  jpa:
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        default_batch_fetch_size: 1000
        format_sql: true
        use_sql_comments: true
    hibernate:
      ddl-auto: create
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: TODO
            client-secret: TODO
            scope: profile, email

logging:
  level:
    com.y2gcoder.app: debug
    org.hibernate.sql: info
    org.hibernate.type.descriptor.sql: trace

app:
  auth:
    token-secret: dGVzdGtleXRlc3RrZXl0ZXN0a2V5dGVzdGtleXRlc3RrZXl0ZXN0a2V5dGVzdGtleXRlc3RrZXkK
    refresh-cookie-key: refreshtoken
    access-token-validity-in-ms: 900000
    refresh-token-validity-in-ms: 1209600000
  oauth2:
    authorized-redirect-uris:
      - http://localhost:3000/oauth2/redirect